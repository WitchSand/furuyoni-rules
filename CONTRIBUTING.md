# 贡献指南

感谢帮助核对规则与改进站点。提交前请先搜索现有 Issue，并选择对应模板。

## 规则翻译纠错

规则纠错必须同时提供：

- 官方综合规则 1.14.1 的 PDF 物理页码；
- 原规则编号或可稳定定位的卡号／表格；
- 日文原文证据与建议修正；
- 对数值、范围、交叉引用、红／绿修订信息的影响说明。

日文底本是规则语义的最高权威。简中来源只能用于译名和表达考据，不能覆盖日文规则含义。8 个已登记的源文异常不得在没有新底本证据时静默修正。

171 条术语均已冻结。任何改名、解锁或规范用法变更，必须先取得项目发起人的新明确决定；社区别名只能用于检索或释义。

## 站点缺陷

请提供页面 URL、浏览器与版本、视口宽度、是否禁用 JavaScript、复现步骤、预期结果和实际结果。若涉及无障碍问题，请说明键盘、焦点、缩放或辅助技术场景。

## Pull Request

1. 从最新 `main` 创建分支；
2. 只提交与问题相关的最小改动；
3. 不提交原 PDF、官方图片、缓存、构建目录、测试报告或本机路径；
4. 运行 `npm run verify:public`；
5. 在 PR 中填写验证结果和权利边界检查。

公开分支采用线性历史，合并前必须通过名为 `quality` 的状态检查。内容修正不会通过强推或改写既有发布历史完成。
